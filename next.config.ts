import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Future: add image domains when real photos are ready
  // images: { domains: [] }
  allowedDevOrigins: ['https://1eba-41-35-16-254.ngrok-free.app'],
  
}

export default nextConfig
